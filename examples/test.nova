// examples/test.nova

// This is a simple program in the Nova language.
// It demonstrates variable declaration, comments, and printing.

let score = 100
let bonus = 25
let totalScore = score + bonus

// Now we check the score
if totalScore > 100 {
    print "You are a winner!"
} else {
    print "Keep trying!"
}


let counter = 0

print "Starting the loop!"

while counter < 5 {
    print counter
    let counter = counter + 1
}

print "Loop finished!"


fun factorial(n) {
    if n < 2 {
        return 1
    }
    return n * factorial(n - 1)
}

print "Calculating factorial of 5..."
let result = factorial(5)
print result